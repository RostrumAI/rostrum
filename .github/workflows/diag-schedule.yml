name: Diagnostic schedule

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scheduled tick at $(date -u +%FT%TZ)"
